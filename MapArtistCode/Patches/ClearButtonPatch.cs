// namespace MapArtist.MapArtistCode.Patches;

// dug through code and may have found method to call as prefix for Clear buttons on press, to patch minor vanilla bug:
//  NMapDrawings.StopDrawingLine()
// public class ClearButtonPatch
// {
    // but not sure and this may already be the logic. am working on something else and will check later.
// }